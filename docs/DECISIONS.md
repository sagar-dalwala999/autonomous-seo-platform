# Decision Register — Canonical Picks (binding for Docs 02–07)

Distilled from the 14 research lanes in `../research/`. Writers: these picks are settled — do not
re-litigate them in a document; present them with their justification (mined from the cited
research file). If a doc-level finding genuinely contradicts a decision here, flag it explicitly
rather than silently deviating. This register is internal — final client docs restate the
decisions in prose/tables, not by referencing this file.

| ID | Area | Decision | Key parameters | Source lane |
|----|------|----------|----------------|-------------|
| D-01 | Runtime | Node.js / TypeScript across the entire Platform | — | all lanes |
| D-02 | Crawler | Crawlee (TS), hybrid static-first: CheerioCrawler default, PlaywrightCrawler escalation via the AdaptivePlaywrightCrawler rendering-type predictor | ~10% sampling learned per site; frontier in Crawlee RequestQueueV2; Firecrawl/Apify = fallback only (per-page economics fail at 100k recrawl scale) | crawling |
| D-03 | Primary datastore | PostgreSQL 16+ with pgvector — single system of record | pages, link edges, keywords, topics, GSC facts, change ledger; embeddings text-embedding-3-small 1536-dim halfvec + HNSW; raw HTML zstd-compressed in S3-compatible object storage | data-model |
| D-04 | Graph analytics | In-process (rustworkx/igraph) in a post-crawl worker; scores written back as columns | PageRank/CheiRank/HITS, BFS depth, orphans, components — a 100k-page graph computes in seconds; NO graph database at MVP | data-model |
| D-05 | Scale triggers | pgvectorscale/Qdrant past ~5–10M vectors; Memgraph only if interactive graph exploration becomes a product feature; OpenSearch only at 100M+-row analytics UIs | trigger-based, not preemptive | data-model |
| D-06 | Page-level queue | BullMQ + Redis for page-level fetch/render/analyze work | site-level jobs; never the crawl frontier | crawling, orchestration |
| D-07 | Orchestration backbone | Temporal (TypeScript SDK); Temporal Cloud at MVP | ~$100/mo floor incl. 1M actions; coarse-phase workflows (~12 phases/site/day — O(10), never O(pages); the concrete P1–P12 enumeration is Doc 03 §4.2), NEVER per-URL workflow steps; durable 14–60-day monitoring timers; signal-based human-approval gates; per-tenant fairness keys. Runner-up: Inngest; self-hosted alt: Hatchet. LLM/agent frameworks are NOT the backbone — LLM calls are plain activities | orchestration |
| D-08 | Detection engine | Deterministic, versioned rulebook (~70 rules) over crawl data; AI never decides issue-hood, only generates fix content | Screaming-Frog-aligned default thresholds, per-project configurable; canonical-cluster-first evaluation; two-source confirmation for negative states; URL Inspection API as budgeted sampling layer (2,000/day/property); IndexNow for non-Google engines | seo-detection |
| D-09 | AI engine shape | Typed-operation emitter, not a content writer: schema-enforced JSON ops (UPDATE_TITLE, ADD_FAQ_BLOCK, ADD_INTERNAL_LINK, …) each carrying an oldValue anchor verified against the live page before apply | provider-native constrained decoding (Anthropic output_config.format / OpenAI json_schema strict / Gemini responseSchema) + validator/re-ask layer (pixel width, keyword coverage, no-new-facts, URL allowlists) | ai-optimization |
| D-10 | Model tiering | Haiku-class bulk → Sonnet-class judgment → Opus-class judging; Batch API (uniform 50% off) + prompt caching | full 10k-page metadata pass at 3,000 in / 500 out per page ≈ **$27.50–$82.50 generation-only**, **$33–$88 all-in** with selective Opus judging (Doc 05 §10.3) — never quote a bare $30–85 for the all-in number; ~$4.50–13.50 per 1k pages analyzed | ai-optimization, cost-security |
| D-11 | Confidence | Computed from deterministic validators + k-sample self-consistency + cross-model judge, calibrated against the Platform's own KEEP/ROLLBACK outcomes; LLM self-reported confidence recorded but never the gate (<0.8 → regenerate/downgrade) | `confidence = 0.55 × soft-validator score + 0.25 × historical acceptance + 0.20 × k-sample self-consistency` (Doc 07 E6.10). Self-report carries **no weight** — audit column plus a sub-0.8 regeneration flag only; perturbing it must leave the score byte-identical | ai-optimization, risk-rollback |
| D-12 | Risk model | Two independent axes — confidence ("is it correct?") × risk ("cost if wrong?") — combined in a decision matrix, never one blended score | Risk = B(type) × blast-radius × traffic-value × velocity modifiers, dampened by earned-trust from site's historical KEEP rate. Bounded form is `risk = clamp(risk_raw × (1 − trust), tier_floor(type), 100)` — per-type **floors** only (D-13); there is no per-type ceiling | risk-rollback |
| D-13 | Hard deny-list | Ten change types no score, trust level or configuration can lower — always human-gated, permanently. **Client's (SPEC §14 HIGH), 4:** robots.txt any edit (B=100), mass redirects/URL restructuring (95), page deletion (95), mass canonicals >10 pages (90) | **Platform extensions on blast-radius grounds, 6** (Doc 02 §2.5 †; the split is shown to the client, not absorbed): noindex insertion *or* removal (90), site-wide template edits (80 — a **hard floor**, not an arithmetic result of the 2.0 blast-radius multiplier), hreflang cluster restructuring (70), host-level redirect policy — HTTP→HTTPS / www resolution (90), server/framework config — `.htaccess`, `next.config` redirects/headers, middleware (90), navigation/architecture changes (80). "per SPEC §14 HIGH" attaches to the four client rows only. The >10-page canonical constant is the deny-list threshold everywhere — no 25-page or 2%-of-site variant | risk-rollback |
| D-14 | Change ledger | Append-only, event-sourced; atomic rollback unit = the batch (one PR / one CMS transaction); content-addressed before/after blobs; drift check before any rollback | doubles with Temporal event history for audit | risk-rollback |
| D-15 | Post-change monitoring | Two-phase: guardrail (days 0–7: crawl-diff, URL Inspection verdicts, HTTP/build errors, CUSUM on fresh GSC) then verdict (day 14–60 by change type: counterfactual from untouched control pages — CausalImpact-style BSTS or DiD + YoY; never naive pre/post). Evaluation clock starts at VERIFIED RECRAWL, not deploy | GSC data lags 2–3 days; Google recrawl "days to weeks" | risk-rollback |
| D-16 | Policy compliance | By construction: per-site change budgets, no autonomous mass-publishing of net-new pages, full AI-change audit trail | scaled-content-abuse / site-reputation-abuse policies (Mar 2024, enforced through 2026 updates) | risk-rollback |
| D-17 | Change application | One Change Application Layer, four adapters: (a) GitHub PR for Next.js/React, (b) WordPress REST + mandatory ~50-line companion plugin, (c) Shopify Admin GraphQL app, (d) edge-worker injection for custom sites | Yoast REST is read-only → companion plugin registers SEO meta with show_in_rest; Shopify: seo fields + global.title_tag/description_tag metafields + urlRedirectCreate, no theme writes in core loop | site-modification |
| D-18 | GitHub integration | GitHub App only (never OAuth App / PATs): per-installation, per-repo, 1-hour down-scoped tokens; commits via GraphQL createCommitOnBranch (signed, expectedHeadOid concurrency guard); one logical SEO change per PR | github-validation |
| D-19 | Code-change generation | Two-tier: deterministic AST codemods (ts-morph/jscodeshift) perform the edit; LLM produces only the VALUES the codemod injects; free-form LLM diffs only where codemods can't express the change, applied search/replace with apply-or-reject | github-validation |
| D-20 | Validation pipeline | Static gates (changed-file allowlist, diff budget, lint, tsc) → sandboxed egress-restricted build (customer npm install treated as hostile/RCE) → preview deploy (Vercel POST /v13/deployments with gitSource; Netlify previews) → SEO assertions on rendered preview: meta-tag diff, Nu HTML Checker, self-built JSON-LD/schema.org validation, Lighthouse CI budgets, lychee links → results as GitHub Checks | Google Rich Results Test has NO public API — plan around it | github-validation |
| D-21 | Rollback mechanics | Two-speed: platform instant rollback (Vercel Instant Rollback / Netlify restore — seconds) for emergencies + git revert PR (GraphQL revertPullRequest) for durable reversal; WordPress = change-ledger value restore (REST revisions have no restore endpoint and don't reliably carry SEO meta — secondary net only); Shopify = ledger-driven inverse writes | github-validation, site-modification |
| D-22 | GSC integration | GSC-first, nightly per-tenant Search Analytics syncs (day-granular; page AND query+page grouped), warehoused permanently (escapes 16-month retention); BigQuery bulk export = enterprise tier; per-customer OAuth webmasters.readonly (sensitive scope, verification needed, no CASA) + service-account-invite fallback | keyword-gsc |
| D-23 | Analytics source | GA4 Data API (free; ~200k tokens/property/day, nightly sync ≈140 tokens) via same OAuth consent (analytics.readonly); conversions = veto-only, last-priority rollback signal (12-day attribution restatement; page-level veto power only at **≥~3.6 key events/day of baseline (≈120–360 organic sessions/day at a 1–3% rate)**, derived in Doc 05 §3 — distinct from, and never to be conflated with, the ~10 organic clicks/day page-level *verdict* power floor); e-commerce cross-check via CMS-native order attribution; server-log connector optional | analytics-integration |
| D-24 | Third-party data | DataForSEO primary (SERP Advanced $0.60/1k, volumes ~$0.06/1k, OnPage/Content Parsing, Labs intent) + Serper for cheap live SERPs; behind a mandatory multi-vendor SerpProvider abstraction; skip Semrush/Ahrefs APIs; never build on Google Keyword Planner | keyword-gsc, competitor |
| D-25 | Opportunity & decay | Ship the researched two-component opportunity score + decay detector — both reproduce the SPEC's worked examples (pos 8.7/32k impressions/2.1% CTR ⇒ HIGH; pos 4→13 + traffic 10k→4.5k ⇒ investigate) | keyword-gsc |
| D-26 | Competitor analysis | DataForSEO SERP + 3-tier fetch ladder (basic → JS-rendered → unlocker; drop unfetchable); one cheap-LLM structured pass per competitor page (~$0.002/page; GLiNER OSS fallback; skip Google NL API); embedding gap set-arithmetic (~0.75 cosine clusters, ≥3-of-10 competitor breadth); five gates before any action (intent, demand evidence, consensus, business relevance, cannibalization); competitor-driven content NEVER auto-applies | ~$0.02–0.05/keyword full analysis | competitor |
| D-27 | Internal linking | Three-channel candidates (exact-phrase/anchor-dictionary, page+paragraph embeddings 0.78–0.85 cosine, GSC query-bridge); score = target-need (PageRank deficit; ~40-inbound cap) × source relevance × placement quality; anchors only from text already on the page, exact-match-once-then-vary ledger; server-side AST insertion via PR/CMS — never JS injection (no detectable SEO effect); tiers T1 auto-PR / T2 one-click / T3 recommend-only; broken-link retarget fully automatic; batch-level CausalImpact, 28-day verdicts | internal-linking |
| D-28 | Frontend | Next.js 16 (App Router, TS), self-hosted Docker; Tailwind + headless components; TanStack Query | no Vercel lock-in | platform-architecture |
| D-29 | API layer | NestJS 11 on the Fastify adapter; OpenAPI generated; tRPC optional internal-only | platform-architecture |
| D-30 | Platform auth | Clerk (Organizations: roles, invitations, verified domains, SAML/OIDC $75/connection); documented exit path Better Auth; Keycloak only for on-prem mandates | platform-architecture |
| D-31 | Tenancy | Pooled multi-tenant Postgres, project_id on every row + RLS as defense-in-depth, non-owner app role; project management = thin domain module (build, don't buy) | platform-architecture, cost-security |
| D-32 | Secrets | KMS envelope encryption, per-tenant data keys under one CMK (~$1/mo + $0.03/10k calls); tokens never in plaintext at rest | cost-security |
| D-33 | Crawler security | Egress-isolated workers; post-DNS-resolution IP validation vs RFC1918/metadata ranges (SSRF) | cost-security, crawling |
| D-34 | Prompt-injection firewall | Models reading crawled/third-party content get no tools and no credentials; closed-vocabulary schema output only; deterministic validation engine before any deploy path (OWASP LLM01 mapping) | cost-security, ai-optimization |
| D-35 | Observability | OpenTelemetry SDK everywhere → Grafana Cloud (free tier MVP) + Sentry Team (~$26/mo) + Temporal Web UI for workflow forensics | orchestration |
| D-36 | Cost envelope | Steady-state: small (100p) ~$20–70/mo · medium (10k p) ~$250–600/mo · large (100k+ p) ~$900–2,800/mo; onboarding one-time <$5 / $60–150 / $700–1,500. Cost strategies are ARCHITECTURE REQUIREMENTS: static-first crawl, content-hash change detection, model tiering, Batch API, prompt caching, sampled recrawls, per-tenant budgets | cost-security |
| D-37 | Automation boundary (headline) | ~14 fix types + 1 protocol action (verified-identical sitemap re-submission, D-40) safely auto-appliable; ~25 in the automated-PR lane; deny-list (D-13) human-gated permanently. Internal linking = strongest "mostly automatable" candidate. The loss function is asymmetric (bad site-wide change ⇒ months of recovery) — all thresholds tuned conservative | seo-detection, risk-rollback, internal-linking |
| D-38 | Cache / Redis-protocol store | **Valkey 9** is the concrete store behind D-06's generic "Redis" — BSD-3 under the Linux Foundation, Redis-7.2.4 lineage, one instance serving BullMQ job state + crawl politeness/dedup + rate limits + locks at MVP | BullMQ's hard requirement is Redis ≥6.2 semantics, which Valkey satisfies; not on BullMQ's formally tested-vendor list, so pin versions and CI-test. ElastiCache Serverless $0.084/GB-hr = −33% vs Redis OSS. Dragonfly = pre-researched scale-up; Upstash's per-command billing is wrong for a polling queue. Refines D-06, does not contradict it | platform-architecture |
| D-39 | Runtime boundary | Node.js/TypeScript for every service **except one Python batch analysis worker** (graph scoring via rustworkx/igraph; GLiNER NER fallback) — the libraries in D-04 have no Node bindings | The worker owns no HTTP surface, no customer credentials, no schema; reads pages/links after a crawl, writes score columns, exits. Cost = one extra container image + dependency manifest. JS-native alternative (graphology) only behind a POC benchmark on a ~10M-edge graph, since the igraph/rustworkx benchmarks do not transfer. Makes D-01's "entire Platform" claim precise | data-model, all lanes |
| D-40 | Sitemap actions | Split by what the action does to the file, not by the word "sitemap": **re-submitting a sitemap verified byte-identical to the live one = LOW auto-apply** (a no-op ping); **regenerating or replacing sitemap contents = MEDIUM**, human-gated, **escalating to HIGH on any net removal of >5% of known indexable URLs**. Replacing the file's contents is never LOW | Never write "MEDIUM-HIGH" — the tier vocabulary is LOW/MEDIUM/HIGH only. Regeneration is always from the crawl's canonical indexable set and diff-validated before submit. Sitemap submission is a **Platform extension** to the client's fixed LOW list (Doc 01 §5.5), so **pending client sign-off the whole action defaults to the MEDIUM gated path** (Doc 05 §2.3). Refines D-37's count (14 fix types + 1 protocol action) and removes the only case that justified a per-type risk ceiling — see D-12 | seo-detection, risk-rollback, site-modification |

## Fixed terminology (use consistently in all docs)

- The product: **"the Platform"** (no invented brand name).
- Risk tiers: **LOW / MEDIUM / HIGH** exactly as SPEC §14 defines them.
- The loop: **Discover → Analyze → Identify → Decide → Optimize → Validate → Deploy → Monitor → Measure → Re-optimize**.
- Documents cross-reference as "Doc 02 (Feasibility)" etc.; requirements as FR-x.y / NFR-x from Doc 01.
